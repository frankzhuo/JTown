缓存穿透
    1.bloomFilter过滤
    2.对空值也缓存

缓存击穿
    单点缓存过期时瞬间大量请求过来，导致DB压力过大。
    利用分布式锁，保证集群中同时只有1个节点查DB。
    TODO:

缓存雪崩
    大量缓存同时过期，会造成DB瞬间压力过大，导致雪崩效应。
    解决方案：例如每次设置缓存过期时间时随机增加0-180秒，避免缓存同时过期。

setNX原理：
